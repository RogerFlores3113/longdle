---
phase: 05-practice-mode
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/Board.tsx
  - src/components/Keyboard.tsx
  - src/components/Key.tsx
  - src/components/Toast.tsx
  - src/App.tsx
autonomous: true
requirements: [PRACTICE-01, PRACTICE-03]
must_haves:
  truths:
    - "Board, Keyboard, Key, and Toast read from GameContext instead of importing useGame directly"
    - "App.tsx provides GameContext.Provider wrapping Board, Keyboard, and Toast"
    - "App.tsx passes useGame selectors to GameContext.Provider value"
    - "useKeyboardListener called with useGame.getState().onKey argument in App.tsx"
    - "Daily game behavior is unchanged — same visual output, same state management"
  artifacts:
    - path: "src/components/Board.tsx"
      provides: "Board component reading from GameContext"
      contains: "useGameContext"
    - path: "src/components/Keyboard.tsx"
      provides: "Keyboard component reading from GameContext"
      contains: "useGameContext"
    - path: "src/components/Key.tsx"
      provides: "Key component reading from GameContext"
      contains: "useGameContext"
    - path: "src/components/Toast.tsx"
      provides: "Toast component reading from GameContext"
      contains: "useGameContext"
    - path: "src/App.tsx"
      provides: "Daily game root with GameContext.Provider"
      contains: "GameContext.Provider"
  key_links:
    - from: "src/App.tsx"
      to: "src/contexts/GameContext.ts"
      via: "GameContext.Provider value={contextValue}"
      pattern: "GameContext.Provider"
    - from: "src/components/Board.tsx"
      to: "src/contexts/GameContext.ts"
      via: "useGameContext()"
      pattern: "useGameContext"
---

<objective>
Refactor Board, Keyboard, Key, and Toast to read from GameContext instead of importing useGame. Then update App.tsx to provide the GameContext with daily game values and pass the onKey argument to useKeyboardListener.

Purpose: This makes the four UI components store-agnostic. Plan 04 (PracticeGame) will provide the same context shape from usePracticeGame — the components will work for both daily and practice without forking.

Output:
- Board.tsx, Keyboard.tsx, Key.tsx, Toast.tsx — each replaces useGame import with useGameContext
- App.tsx — adds GameContext.Provider wrapper and fixes useKeyboardListener call signature
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-practice-mode/05-CONTEXT.md
@.planning/phases/05-practice-mode/05-PATTERNS.md
@.planning/phases/05-practice-mode/05-01-SUMMARY.md

<interfaces>
<!-- From GameContext.ts (created in plan 01) -->
```typescript
export interface GameContextValue {
  guesses: ScoredGuess[]
  currentGuess: string
  gameStatus: GameStatus
  isAnimating: boolean
  toastMessage: string | null
  rowShakeKey: number
  keyStatuses: Record<string, KeyStatus>
  onKey: (key: string) => void
}

export const GameContext: React.Context<GameContextValue | null>
export function useGameContext(): GameContextValue
```

<!-- Current component import patterns to replace -->
<!-- Board.tsx currently: import { useGame } from '../hooks/useGame' -->
<!-- Keyboard.tsx currently: import { useGame } from '../hooks/useGame' -->
<!-- Key.tsx currently: import { useGame } from '../hooks/useGame' -->
<!-- Toast.tsx currently: import { useGame } from '../hooks/useGame' -->

<!-- App.tsx currently calls: useKeyboardListener() with no arguments -->
<!-- After plan 01, useKeyboardListener requires: useKeyboardListener(onKey) -->

<!-- App.tsx currently has no GameContext.Provider -->
<!-- After this plan, App.tsx must wrap main and Toast with GameContext.Provider -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Board, Keyboard, Key, Toast to use GameContext</name>
  <files>src/components/Board.tsx, src/components/Keyboard.tsx, src/components/Key.tsx, src/components/Toast.tsx</files>
  <read_first>
    - src/components/Board.tsx — read the FULL file to see every useGame selector call before modifying
    - src/components/Keyboard.tsx — read the FULL file
    - src/components/Key.tsx — read the FULL file
    - src/components/Toast.tsx — read the FULL file
    - src/contexts/GameContext.ts — read to confirm exact export names and useGameContext signature (created in plan 01)
  </read_first>
  <action>
Make the following targeted changes to each component. Read each file fully before editing; the exact line numbers may differ from research estimates.

**Board.tsx changes:**
1. Remove: `import { useGame } from '../hooks/useGame'`
2. Add: `import { useGameContext } from '../contexts/GameContext'`
3. Replace all `useGame((s) => s.guesses)`, `useGame((s) => s.currentGuess)`, `useGame((s) => s.gameStatus)`, `useGame((s) => s.rowShakeKey)`, `useGame((s) => s.isAnimating)` selector calls with a single destructure: `const { guesses, currentGuess, gameStatus, rowShakeKey, isAnimating } = useGameContext()`
4. Remove any remaining `useGame` imports or calls from Board.tsx

**Keyboard.tsx changes:**
1. Remove: `import { useGame } from '../hooks/useGame'`
2. Add: `import { useGameContext } from '../contexts/GameContext'`
3. Replace `const keyStatuses = useGame((s) => s.keyStatuses)` (or equivalent selector) with `const { keyStatuses } = useGameContext()`
4. Remove any remaining `useGame` imports or calls

**Key.tsx changes:**
1. Remove: `import { useGame } from '../hooks/useGame'`
2. Add: `import { useGameContext } from '../contexts/GameContext'`
3. Replace `const onKey = useGame((s) => s.onKey)` (or equivalent) with `const { onKey } = useGameContext()`
4. The `onPointerDown={() => onKey(value)}` usage is UNCHANGED — only the source of `onKey` changes
5. Remove any remaining `useGame` imports or calls

**Toast.tsx changes:**
1. Remove: `import { useGame } from '../hooks/useGame'`
2. Add: `import { useGameContext } from '../contexts/GameContext'`
3. Replace `const message = useGame((s) => s.toastMessage)` (or equivalent) with `const { toastMessage: message } = useGameContext()`
4. Remove any remaining `useGame` imports or calls

After all four edits, verify TypeScript compiles with no errors in these files (App.tsx errors from plan 01 are still expected until Task 2 in this plan fixes them).
  </action>
  <verify>
    <automated>cd /home/rflor/longdle && npx tsc --noEmit 2>&1 | grep -E 'Board|Keyboard|Key\.tsx|Toast' | head -20</automated>
  </verify>
  <done>
    - grep for "from '../hooks/useGame'" in Board.tsx, Keyboard.tsx, Key.tsx, Toast.tsx returns no matches: `grep -rl "from '../hooks/useGame'" src/components/Board.tsx src/components/Keyboard.tsx src/components/Key.tsx src/components/Toast.tsx` → no output
    - grep for "useGameContext" in all four files returns matches in each: `grep -l "useGameContext" src/components/Board.tsx src/components/Keyboard.tsx src/components/Key.tsx src/components/Toast.tsx` → 4 files listed
    - No TypeScript errors in Board.tsx, Keyboard.tsx, Key.tsx, Toast.tsx individually
  </done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx — add GameContext.Provider and fix useKeyboardListener call</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx — read the FULL file before modifying (current structure: lines 1–133)
    - src/contexts/GameContext.ts — read for GameContext and GameContextValue import names
    - src/hooks/useGame.ts — read lines 35–48 (GameState interface) to confirm all fields available as selectors for the context value
    - src/hooks/useKeyboardListener.ts — read to confirm the new signature: useKeyboardListener(onKey: (key: string) => void)
  </read_first>
  <action>
Make the following changes to `src/App.tsx`:

**1. Add imports** (after existing import block):
```typescript
import { GameContext } from './contexts/GameContext'
import type { GameContextValue } from './contexts/GameContext'
```

**2. Fix useKeyboardListener call** — change line 20 from:
```typescript
useKeyboardListener()
```
to:
```typescript
useKeyboardListener(useGame.getState().onKey)
```

Note: `useGame.getState().onKey` is stable (it's a Zustand action, not a reactive value). This matches the pattern from PATTERNS.md. If TypeScript shows a react-hooks/exhaustive-deps warning, that is acceptable — the onKey function reference is stable across renders.

**3. Add additional useGame selectors** needed for the context value. After the existing selectors (gameStatus, isAnimating on lines ~25–26), add:
```typescript
const guesses = useGame((s) => s.guesses)
const currentGuess = useGame((s) => s.currentGuess)
const toastMessage = useGame((s) => s.toastMessage)
const rowShakeKey = useGame((s) => s.rowShakeKey)
const keyStatuses = useGame((s) => s.keyStatuses)
const onKey = useGame((s) => s.onKey)
```

**4. Build the context value** — add this after the selector declarations, before the useEffect calls:
```typescript
const gameContextValue: GameContextValue = {
  guesses,
  currentGuess,
  gameStatus,
  isAnimating,
  toastMessage,
  rowShakeKey,
  keyStatuses,
  onKey,
}
```

**5. Wrap the component output** — in the JSX return, wrap `<main className="app__main">`, `<Board />`, `<Keyboard />`, and `<Toast />` with `<GameContext.Provider value={gameContextValue}>`. The wrapping must include both the `<main>` block AND the `<Toast />` element so all four refactored components receive context.

The wrapped structure in the return JSX must look like:
```tsx
<GameContext.Provider value={gameContextValue}>
  <main className="app__main">
    <Board />
    <Keyboard />
  </main>
  <Toast />
</GameContext.Provider>
```

Keep all modal conditionals OUTSIDE the GameContext.Provider — modals read from useGame directly and do not need context.

**6. Verify** the colorblindMode effect, hasSeenHowToPlay effect, closeModal function, handleShare function, and all modal JSX are unchanged and still present.
  </action>
  <verify>
    <automated>cd /home/rflor/longdle && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - GameContext.Provider present in App.tsx: `grep -c 'GameContext.Provider' src/App.tsx` → 1
    - useKeyboardListener called with argument: `grep 'useKeyboardListener' src/App.tsx` → shows `useKeyboardListener(useGame.getState().onKey)`
    - gameContextValue declared in App.tsx: `grep -c 'gameContextValue' src/App.tsx` → 2 or more (declaration + usage)
    - TypeScript compiles with 0 errors: `npx tsc --noEmit` exits 0
    - npm test passes with no regressions: `npm test` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameContext.Provider → consumer components | Context value flows from store to components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Elevation of Privilege | useGameContext throw | accept | Throwing on missing Provider is intentional defensive programming; error is only reachable during dev if a component is rendered outside its tree |
| T-05-06 | Spoofing | GameContext value | accept | Context value is derived directly from the Zustand store; no user-controlled data enters the context value shape |
</threat_model>

<verification>
After both tasks complete:

1. No useGame imports in the four UI components:
   `grep -r "from '../hooks/useGame'" src/components/Board.tsx src/components/Keyboard.tsx src/components/Key.tsx src/components/Toast.tsx` → no output

2. All four components import useGameContext:
   `grep -rl "useGameContext" src/components/` → lists Board.tsx, Keyboard.tsx, Key.tsx, Toast.tsx

3. GameContext.Provider in App.tsx:
   `grep -c 'GameContext.Provider' src/App.tsx` → 1

4. useKeyboardListener receives argument in App.tsx:
   `grep 'useKeyboardListener' src/App.tsx` → contains "useGame.getState().onKey"

5. Full TypeScript compile passes:
   `npx tsc --noEmit` → exit code 0

6. All tests pass:
   `npm test` → exit code 0
</verification>

<success_criteria>
- Board, Keyboard, Key, Toast have zero useGame imports — all store access goes through useGameContext
- App.tsx provides GameContext.Provider to the component subtree containing Board, Keyboard, Toast
- Daily game behavior is visually and functionally unchanged from Phase 4 state
- npm test passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-practice-mode/05-03-SUMMARY.md`
</output>
