---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.app.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/test/setup.ts
  - .gitignore
autonomous: true
requirements: [GAME-01]  # bootstrap enables the visible 6x7 grid that Plan 05 renders; toolchain plan
must_haves:
  truths:
    - "`npm run dev` starts a Vite dev server on localhost without errors"
    - "`npm run build` produces a production build without errors"
    - "`npm test` runs Vitest and reports 0 tests (no test files yet) without configuration errors"
    - "Tailwind v4 utility classes (e.g. `bg-red-500`) compile and apply when used in a JSX file"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "zustand"
    - path: "vite.config.ts"
      provides: "Vite + Tailwind v4 plugin + Vitest jsdom config"
      contains: "@tailwindcss/vite"
    - path: "src/index.css"
      provides: "Tailwind v4 import"
      contains: "@import \"tailwindcss\""
    - path: "src/test/setup.ts"
      provides: "Vitest jest-dom matcher registration"
      contains: "@testing-library/jest-dom/vitest"
  key_links:
    - from: "vite.config.ts"
      to: "Tailwind v4 plugin"
      via: "plugins: [react(), tailwindcss()]"
      pattern: "tailwindcss\\(\\)"
    - from: "vite.config.ts"
      to: "Vitest jsdom"
      via: "test config block"
      pattern: "environment:\\s*'jsdom'"
---

<objective>
Bootstrap the Longdle Vite + React 19 + TypeScript project with Tailwind v4 and Vitest. This plan produces an empty but fully wired toolchain — `npm run dev`, `npm run build`, `npm test` all pass on a near-empty `App.tsx`. No game code yet — those come in Plan 02 onwards.

Purpose: D-04, D-14 step 0. Every subsequent plan depends on this scaffold. Verifies stack assumptions (A3, A5) before any logic is written.

Output: Vite project at repo root, configured per RESEARCH.md "Standard Stack" section, with Vitest + Tailwind v4 working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- Greenfield — no existing interfaces. This plan creates the build foundation. -->

Verified library versions (RESEARCH.md Standard Stack, npm view 2026-05-04):
- vite: 8.0.10
- react / react-dom: 19.2.5
- typescript: latest from vite react-ts template (5.x)
- zustand: 5.0.13
- tailwindcss: 4.2.4
- @tailwindcss/vite: 4.2.4
- vitest: 4.1.5
- @testing-library/react: 16.3.2
- @testing-library/user-event: latest
- @testing-library/jest-dom: latest
- jsdom: 29.1.1

Tailwind v4 import (RESEARCH.md): `@import "tailwindcss";` — NOT v3 directives.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React 19 + TypeScript project at repo root</name>
  <files>package.json, package-lock.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/App.tsx, src/index.css, src/vite-env.d.ts, .gitignore, public/vite.svg, src/assets/react.svg</files>
  <read_first>
    - CLAUDE.md (Stack section + Build Order)
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack table — MUST honor exact verified versions)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04 bootstrap command)
  </read_first>
  <action>
Run `npm create vite@latest longdle -- --template react-ts` from a temporary location, then move the generated files into the repo root `/home/rflor/longdle/` (the repo root already has CLAUDE.md and .planning/ — those must NOT be overwritten). Alternative approach: scaffold directly with `npm create vite@latest . -- --template react-ts` from inside the repo root, accepting any prompts that ask about non-empty directory (use `--force` flag if needed to overwrite Vite's defaults but preserve CLAUDE.md / .planning/ / .git/).

After scaffold:
1. Run `npm install` to install template defaults.
2. Verify `package.json` contains `"react": "^19...."` and `"react-dom": "^19...."`. If template scaffolds React 18, run `npm install react@^19 react-dom@^19` to upgrade per CLAUDE.md stack requirement.
3. Verify `tsconfig.json` exists; do not modify yet (Task 2 adjusts it if needed).
4. Confirm the repo root layout: `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/index.css`, plus existing `CLAUDE.md`, `.planning/`, `.git/`.

Do NOT modify `.planning/`, `CLAUDE.md`, `.git/` during scaffold. If the Vite scaffolder refuses to write into a non-empty dir, scaffold to a temp dir then move files in.

Update `.gitignore` (the Vite-generated one is fine) to also include: `node_modules`, `dist`, `coverage`, `*.local`, `.env*` — append any missing entries.
  </action>
  <verify>
    <automated>test -f /home/rflor/longdle/package.json && test -f /home/rflor/longdle/vite.config.ts && test -f /home/rflor/longdle/src/main.tsx && grep -q '"react": "\^19' /home/rflor/longdle/package.json && grep -q '"react-dom": "\^19' /home/rflor/longdle/package.json && test -d /home/rflor/longdle/node_modules</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at `/home/rflor/longdle/package.json`
    - `package.json` contains `"react": "^19` and `"react-dom": "^19`
    - `vite.config.ts` exists at repo root
    - `tsconfig.json` exists at repo root
    - `src/main.tsx` exists
    - `src/App.tsx` exists
    - `index.html` exists at repo root
    - `node_modules/` populated (npm install ran)
    - `.planning/` directory still present and unmodified
    - `CLAUDE.md` still present at repo root with original content (`grep -q "two-pass algorithm" /home/rflor/longdle/CLAUDE.md` succeeds)
  </acceptance_criteria>
  <done>Vite react-ts template scaffolded at repo root, React 19 confirmed, dependencies installed, no project artifacts overwritten.</done>
</task>

<task type="auto">
  <name>Task 2: Install Phase 1 dependencies and configure Vite + Tailwind v4 + Vitest</name>
  <files>package.json, vite.config.ts, src/index.css, src/test/setup.ts, src/App.tsx, tsconfig.json, tsconfig.app.json</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack — Installation section + vite.config.ts snippet + src/test/setup.ts snippet)
    - vite.config.ts (current state from Task 1)
    - src/index.css (current state from Task 1)
    - tsconfig.json (current state)
  </read_first>
  <action>
Step 1 — Install runtime + dev dependencies (D-04):
```
npm install zustand@^5
npm install -D vitest@^4 @testing-library/react @testing-library/user-event @testing-library/jest-dom jsdom
npm install tailwindcss @tailwindcss/vite
```

Step 2 — Replace `vite.config.ts` content with this exact content (RESEARCH.md verified pattern):
```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
  },
})
```

Step 3 — Add Vitest types reference. Append to `tsconfig.app.json` `compilerOptions.types` array: `"vitest/globals"`. If the array doesn't exist, add `"types": ["vitest/globals"]` under compilerOptions.

Step 4 — Replace `src/index.css` content with exactly:
```css
@import "tailwindcss";
```
(This replaces any v3 `@tailwind base/components/utilities` directives or default Vite CSS — see RESEARCH.md Pitfall 6.)

Step 5 — Create `src/test/setup.ts` with exactly:
```ts
import '@testing-library/jest-dom/vitest'
```

Step 6 — Add npm scripts to `package.json`. The `scripts` object MUST contain at minimum:
```json
{
  "dev": "vite",
  "build": "tsc -b && vite build",
  "preview": "vite preview",
  "test": "vitest run",
  "test:watch": "vitest"
}
```
(Vite template scaffolds `dev`, `build`, `preview`. Add `test` and `test:watch`. Keep any `lint` script the template adds.)

Step 7 — Replace `src/App.tsx` content with a minimal placeholder that proves Tailwind works:
```tsx
function App() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gray-50">
      <h1 className="text-2xl font-semibold text-gray-900">Longdle</h1>
    </main>
  )
}

export default App
```
Remove `src/App.css` if it exists (Vite template often scaffolds it; we use Tailwind only).

Step 8 — In `src/main.tsx`, ensure `import './index.css'` is present (Vite template includes it). Remove any `import './App.css'` line.
  </action>
  <verify>
    <automated>cd /home/rflor/longdle && grep -q '"zustand"' package.json && grep -q '"tailwindcss"' package.json && grep -q '"@tailwindcss/vite"' package.json && grep -q '"vitest"' package.json && grep -q '"jsdom"' package.json && grep -q "tailwindcss()" vite.config.ts && grep -q "environment: 'jsdom'" vite.config.ts && grep -q '@import "tailwindcss"' src/index.css && grep -q "@testing-library/jest-dom/vitest" src/test/setup.ts && grep -q '"test": "vitest run"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include: `zustand`, `tailwindcss`, `@tailwindcss/vite`
    - `package.json` devDependencies include: `vitest`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`, `jsdom`
    - `vite.config.ts` imports `@tailwindcss/vite` and includes `tailwindcss()` in plugins array
    - `vite.config.ts` includes `test: { environment: 'jsdom', globals: true, setupFiles: ['./src/test/setup.ts'] }` block
    - `src/index.css` content is exactly `@import "tailwindcss";` (no v3 directives)
    - `src/test/setup.ts` exists with `@testing-library/jest-dom/vitest` import
    - `package.json` scripts.test === `"vitest run"`
    - `src/App.tsx` renders an `<h1>` with text "Longdle"
  </acceptance_criteria>
  <done>All Phase 1 deps installed, Vite + Tailwind v4 + Vitest fully configured.</done>
</task>

<task type="auto">
  <name>Task 3: Verify toolchain — dev, build, and test commands all succeed</name>
  <files>(none — this task only runs commands and inspects output)</files>
  <read_first>
    - package.json (to confirm scripts exist)
    - vite.config.ts (to confirm config is valid)
  </read_first>
  <action>
Step 1 — Run `npm run build`. Must exit 0. If TypeScript errors appear, fix them in App.tsx / main.tsx (do NOT add `any` types or `// @ts-ignore`; fix the underlying issue). Common fixes: ensure `tsconfig.app.json` `include` covers `src` and `src/test/setup.ts`.

Step 2 — Run `npm test`. Vitest must report `No test files found` (or similar zero-test summary) WITHOUT a config error. Exit code may be non-zero (Vitest exits 1 when no tests match by default). To confirm config validity rather than test count, run `npx vitest run --passWithNoTests`. This MUST exit 0.

Step 3 — Update `package.json` `scripts.test` to `"vitest run --passWithNoTests"` so future plans (and CI) can invoke `npm test` without false-failing on empty suites. Plan 03 will add real tests; this guard is removed once tests exist (or kept — both behaviors are fine).

Step 4 — Run `npm run dev` in background, give it 3 seconds to start, then curl `http://localhost:5173` (the Vite default port). Response must be HTTP 200 and the body must contain `<div id="root">`. Kill the dev server after the check.

Run command:
```bash
npm run dev &
DEV_PID=$!
sleep 3
curl -s -o /tmp/longdle-dev.html -w "%{http_code}" http://localhost:5173
kill $DEV_PID
grep -q '<div id="root">' /tmp/longdle-dev.html
```
  </action>
  <verify>
    <automated>cd /home/rflor/longdle && npm run build 2>&1 | tail -5 && npx vitest run --passWithNoTests 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/` directory exists after build
    - `dist/index.html` exists after build
    - `npx vitest run --passWithNoTests` exits 0
    - `package.json` scripts.test contains `--passWithNoTests` flag (or comparable Vitest 4 equivalent)
    - Dev server (briefly started) returns HTTP 200 with `<div id="root">` in body — recorded in plan summary even if not in automated grep
  </acceptance_criteria>
  <done>Vite dev server, build, and Vitest all run successfully on the empty App.tsx.</done>
</task>

</tasks>

<verification>
Toolchain integrity:
- `npm run build` exits 0
- `npx vitest run --passWithNoTests` exits 0
- Tailwind v4 utility classes work (visible at dev server when classes added to App.tsx)
- React 19 confirmed in `package.json`

This plan unblocks Plan 02 (words.ts curation), which is the next wave.
</verification>

<success_criteria>
- Repo root contains a working Vite + React 19 + TS scaffold
- All Phase 1 dependencies installed at versions matching RESEARCH.md Standard Stack
- Tailwind v4 configured via `@tailwindcss/vite` plugin (NOT v3 patterns)
- Vitest configured with jsdom + jest-dom matchers
- Build, dev, and test commands all green
- Existing `.planning/` and `CLAUDE.md` untouched
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` listing:
- Exact installed versions (from `npm list --depth=0`)
- Any deviations from RESEARCH.md Standard Stack (e.g., if a peer-dep warning forced a version pin — Assumption A3)
- Confirmation that Tailwind v4 + Vitest jsdom both work
</output>
</content>
</invoke>