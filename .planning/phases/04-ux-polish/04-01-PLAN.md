---
phase: 04-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/tiles.css
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02

must_haves:
  truths:
    - "Background color is #122012 (visibly lighter than the previous #0d1b0d)"
    - "Keyboard surface color is #1f341f (visibly lighter than the previous #1a2f1a)"
    - "Toast displays as a near-white pill (#f0f0e8 background) with dark green text (#0d1b0d) — readable at a glance"
  artifacts:
    - path: "src/styles/tiles.css"
      provides: "Updated color tokens and toast rule"
      contains: "#122012"
  key_links:
    - from: "src/styles/tiles.css :root"
      to: "body background"
      via: "var(--color-bg)"
      pattern: "--color-bg: #122012"
    - from: "src/styles/tiles.css :root"
      to: ".key background"
      via: "var(--color-surface)"
      pattern: "--color-surface: #1f341f"
    - from: "src/styles/tiles.css .toast"
      to: "toast readability"
      via: "hardcoded background + color"
      pattern: "background: #f0f0e8"
---

<objective>
Nudge the two dark background tokens slightly lighter (+5 RGB each) and fix the unreadable toast (white-on-white bug).

Purpose: These are the two most immediately noticeable visual regressions — the palette is too dark to read comfortably, and the toast messages are invisible (both background and text resolve to near-white via the same token).

Output: src/styles/tiles.css with 3 changed lines. No other files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ux-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nudge background tokens + fix toast colors in tiles.css</name>
  <files>src/styles/tiles.css</files>
  <read_first>
    src/styles/tiles.css — read the full file before making any changes so you see the exact current values and rule structure
  </read_first>
  <action>
Make exactly 3 line changes in src/styles/tiles.css:

**Change 1 — --color-bg token (POLISH-01, D-01):**
Find in :root block:
  --color-bg: #0d1b0d;
Replace with:
  --color-bg: #122012;

**Change 2 — --color-surface token (POLISH-01, D-02):**
Find in :root block:
  --color-surface: #1a2f1a;
Replace with:
  --color-surface: #1f341f;

**Change 3 — .toast color fix (POLISH-02, D-04, D-05, D-06):**
Find the .toast rule. Currently:
  background: var(--color-text);
  color: var(--color-text-inverse);
Replace those two declarations with:
  background: #f0f0e8;
  color: #0d1b0d;

Do NOT add new CSS custom properties. Do NOT change border-radius, padding, position, or any other .toast property. Do NOT change any other color token. The six tokens --color-correct, --color-present, --color-absent, --color-border, --color-text, --color-text-inverse are UNCHANGED.
  </action>
  <verify>
    <automated>grep -n "color-bg\|color-surface\|\.toast" src/styles/tiles.css</automated>
  </verify>
  <acceptance_criteria>
    - grep for `--color-bg:` in src/styles/tiles.css returns exactly `--color-bg: #122012;`
    - grep for `--color-surface:` returns exactly `--color-surface: #1f341f;`
    - grep for `background:` inside the .toast rule returns `background: #f0f0e8;`
    - grep for `color:` inside the .toast rule returns `color: #0d1b0d;`
    - grep for `var(--color-text)` or `var(--color-text-inverse)` inside the .toast rule returns no results
    - grep for `--color-correct:` still returns `--color-correct: #538d4e;` (unchanged)
    - grep for `--color-text:` still returns `--color-text: #f0f0e8;` (unchanged)
  </acceptance_criteria>
  <done>tiles.css has the 3 changed lines and all other tokens are intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS source → browser render | Static file, no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | tiles.css | accept | Static CSS file served by Vite/Vercel — no executable code, no user input |
</threat_model>

<verification>
Run the build to confirm no CSS errors:

```bash
npm run build 2>&1 | tail -20
```

Expect: build succeeds with no errors.

Spot-check token values:
```bash
grep -E "color-bg|color-surface" src/styles/tiles.css
grep -A5 "\.toast {" src/styles/tiles.css
```
</verification>

<success_criteria>
- `--color-bg: #122012` present in :root
- `--color-surface: #1f341f` present in :root
- `.toast` rule has `background: #f0f0e8` and `color: #0d1b0d` (hardcoded, no var())
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-polish/04-01-SUMMARY.md`
</output>
