---
phase: 02-features
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - src/components/WinAnimation.tsx
  - src/components/HowToPlayModal.tsx
  - src/components/StatsModal.tsx
  - src/components/SettingsModal.tsx
autonomous: true
requirements:
  - V3-01
  - ONBOARD-02
  - SHARE-03
  - SHARE-04
  - SETTINGS-01
  - SETTINGS-02
  - SETTINGS-03

must_haves:
  truths:
    - "WinAnimation renders null; has JSDoc v3 annotation; interface has dayIndex, won, guessCount props"
    - "HowToPlayModal shows three example tiles (40×40px divs) with correct/present/absent colored backgrounds"
    - "StatsModal reads readStats() directly (no Zustand); renders 4 stat numbers and 7 histogram bars"
    - "Histogram bars have minimum 8% width; current game's bar highlighted with .stats-bar--highlight when won"
    - "SettingsModal hard mode toggle disabled (opacity 0.5, pointer-events none) when guesses.length > 0"
    - "SettingsModal colorblind toggle always interactive"
    - "Share button in StatsModal calls generateShareText synchronously and passes to navigator.clipboard.writeText() with no await before writeText"
  artifacts:
    - path: "src/components/WinAnimation.tsx"
      provides: "No-op v3 stub with WinAnimationProps interface"
      exports: ["WinAnimation"]
    - path: "src/components/HowToPlayModal.tsx"
      provides: "How-to-play content with example tiles"
      exports: ["HowToPlayModal"]
    - path: "src/components/StatsModal.tsx"
      provides: "Stats + histogram + share button"
      exports: ["StatsModal"]
    - path: "src/components/SettingsModal.tsx"
      provides: "Hard mode + colorblind toggles"
      exports: ["SettingsModal"]
  key_links:
    - from: "StatsModal share button"
      to: "src/lib/share.ts generateShareText"
      via: "synchronous call in click handler, result passed to clipboard.writeText()"
      pattern: "generateShareText"
    - from: "SettingsModal"
      to: "src/hooks/useGame.ts useSettings"
      via: "useSettings() destructuring hardMode, colorblindMode, guesses.length"
      pattern: "useSettings"
    - from: "StatsModal, SettingsModal, HowToPlayModal"
      to: "src/components/Modal.tsx"
      via: "wraps all content inside <Modal onClose={onClose}>"
      pattern: "import.*Modal.*from.*Modal"
---

<objective>
Create the four non-EndGame modal content components: WinAnimation stub, HowToPlayModal, StatsModal, and SettingsModal. All four wrap their content in the `<Modal>` component from plan 03.

Purpose: These modals cover onboarding (ONBOARD-01/02), lifetime stats (SHARE-03/04), settings (SETTINGS-01/02/03), and the v3 animation slot (V3-01). EndGame modal gets its own plan (05) because it also owns the share button primary CTA and the WinAnimation placement.

Output: 4 component files ready for App.tsx integration in plan 07.
</objective>

<execution_context>
@/home/rflor/longdle/.planning/phases/02-features/02-04-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/rflor/longdle/.planning/ROADMAP.md
@/home/rflor/longdle/.planning/phases/02-features/02-CONTEXT.md
@/home/rflor/longdle/.planning/phases/02-features/02-UI-SPEC.md
@/home/rflor/longdle/CLAUDE.md

<interfaces>
<!-- From src/hooks/useGame.ts (plan 02 extended it) -->
```typescript
export interface SettingsState {
  version: number
  hardMode: boolean
  colorblindMode: boolean
  hasSeenHowToPlay: boolean
  setHardMode: (v: boolean) => void
  setColorblindMode: (v: boolean) => void
  setHasSeenHowToPlay: (v: boolean) => void
}

export interface GameState {
  guesses: ScoredGuess[]
  gameStatus: GameStatus
  dayIndex: number
  // ...more fields
}

// Access patterns:
const { hardMode, colorblindMode, setHardMode, setColorblindMode } = useSettings()
const guesses = useGame((s) => s.guesses)
const gameStatus = useGame((s) => s.gameStatus)
const dayIndex = useGame((s) => s.dayIndex)
```

<!-- From src/lib/storage.ts -->
```typescript
export interface Stats {
  version: number
  gamesPlayed: number
  gamesWon: number
  currentStreak: number
  maxStreak: number
  guessDistribution: number[]  // length 7: [wins-in-1, wins-in-2, ..., wins-in-6, losses]
}
export function readStats(): Stats  // synchronous localStorage read
```

<!-- From src/lib/share.ts (plan 01) -->
```typescript
export function generateShareText(
  guesses: ScoredGuess[],
  dayIndex: number,
  colorblindMode: boolean
): string  // synchronous pure function
```

<!-- From src/components/Modal.tsx (plan 03) -->
```tsx
interface ModalProps {
  onClose: () => void
  children: React.ReactNode
}
export function Modal({ onClose, children }: ModalProps): JSX.Element
// Usage: <Modal onClose={onClose}><button className="modal-close" onClick={onClose}>×</button>...content</Modal>
```

<!-- CSS classes from tiles.css (plans 02+03 added these) -->
<!-- .modal-close, .modal-panel, .modal-backdrop — from plan 03 -->
<!-- html.colorblind { --color-correct: #f5793a; --color-present: #85c0f9 } — from plan 02 -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WinAnimation stub, HowToPlayModal, and SettingsModal</name>
  <files>
    src/components/WinAnimation.tsx
    src/components/HowToPlayModal.tsx
    src/components/SettingsModal.tsx
  </files>
  <read_first>
    - src/components/Modal.tsx — Modal wrapper component signature and usage pattern
    - src/hooks/useGame.ts — useSettings fields available: hardMode, colorblindMode, setHardMode, setColorblindMode, hasSeenHowToPlay
    - src/styles/tiles.css — existing CSS class names; no new utility classes needed here
  </read_first>
  <action>
    Create three files. Read each relevant source file first, then implement.

    ---

    **src/components/WinAnimation.tsx** (D-20, D-21, V3-01):

    ```tsx
    /**
     * WinAnimation — v3 placeholder.
     * Receives game result props; currently renders nothing.
     * v3 will replace this with red panda pixel art animations
     * triggered when the player wins the daily puzzle.
     *
     * Integration point: rendered inside EndGameModal when gameStatus === 'won'.
     * Props are passed through so v3 can use them without changing EndGameModal.
     */

    export interface WinAnimationProps {
      dayIndex: number
      won: boolean
      guessCount: number
    }

    export function WinAnimation(_props: WinAnimationProps) {
      return null
    }
    ```

    ---

    **src/components/HowToPlayModal.tsx** (ONBOARD-01, ONBOARD-02, D-12, D-13):

    Props: `interface HowToPlayModalProps { onClose: () => void }`

    Structure (uses Modal wrapper):
    ```tsx
    import { Modal } from './Modal'

    export function HowToPlayModal({ onClose }: { onClose: () => void }) {
      return (
        <Modal onClose={onClose}>
          <button className="modal-close" onClick={onClose} aria-label="Close">×</button>
          <h2 style={{ textAlign: 'center', fontSize: 24, fontWeight: 600, margin: '0 0 16px' }}>
            How To Play
          </h2>
          <p style={{ fontSize: 16, lineHeight: 1.5, margin: '0 0 16px' }}>
            Guess the <strong>LONGDLE</strong> in 7 tries.
            Each guess must be a valid 6-letter word.
          </p>
          <p style={{ fontSize: 12, fontWeight: 600, textTransform: 'uppercase', margin: '0 0 12px', letterSpacing: '0.05em' }}>
            Examples
          </p>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
              <div className="howtoplay-tile howtoplay-tile--correct">W</div>
              <span style={{ fontSize: 16 }}><strong>W</strong> is in the right spot</span>
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
              <div className="howtoplay-tile howtoplay-tile--present">I</div>
              <span style={{ fontSize: 16 }}><strong>I</strong> is in the word, but in the wrong spot</span>
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
              <div className="howtoplay-tile howtoplay-tile--absent">U</div>
              <span style={{ fontSize: 16 }}><strong>U</strong> is not in the word</span>
            </div>
          </div>
        </Modal>
      )
    }
    ```

    Also add to src/styles/tiles.css (append after .modal-close:hover block):
    ```css
    /* ───────── HowToPlay example tiles (D-13) ───────── */

    .howtoplay-tile {
      width: 40px;
      height: 40px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      font-weight: 600;
      text-transform: uppercase;
      color: var(--color-text-inverse);
      flex-shrink: 0;
    }

    .howtoplay-tile--correct { background: var(--color-correct); }
    .howtoplay-tile--present { background: var(--color-present); }
    .howtoplay-tile--absent  { background: var(--color-absent); }
    ```

    ---

    **src/components/SettingsModal.tsx** (SETTINGS-01, SETTINGS-02, D-14, D-15):

    Reads: `useSettings()` for hardMode, colorblindMode, setHardMode, setColorblindMode.
    Reads: `useGame((s) => s.guesses)` for guesses.length to disable hard mode toggle.

    Toggle implementation: use a styled `<button role="switch" aria-checked={value}>` pattern — simpler than visually-hidden checkbox; easier to style with CSS.

    NOTE: Using `<button role="switch">` (not `<input type="checkbox">`) — this is intentional. The CSS
    selectors use `[aria-checked="true"]` and `.settings-toggle--disabled` attribute selectors rather
    than `:checked`/`:disabled` pseudo-classes. Any PATTERNS.md reference to `input[type=checkbox]`
    semantics does NOT apply here — do not switch to checkbox inputs.

    Add to tiles.css (append after howtoplay-tile rules):
    ```css
    /* ───────── Settings modal (D-14, D-15) ───────── */

    .settings-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 16px 0;
    }

    .settings-row + .settings-row {
      border-top: 1px solid var(--color-border);
    }

    .settings-toggle {
      position: relative;
      width: 48px;
      height: 24px;
      background: var(--color-surface);
      border: 2px solid var(--color-border);
      border-radius: 12px;
      cursor: pointer;
      flex-shrink: 0;
      transition: background 0.15s ease, border-color 0.15s ease;
    }

    .settings-toggle[aria-checked="true"] {
      background: var(--color-correct);
      border-color: var(--color-correct);
    }

    .settings-toggle::after {
      content: '';
      position: absolute;
      top: 2px;
      left: 4px;
      width: 16px;
      height: 16px;
      background: #ffffff;
      border-radius: 50%;
      transition: left 0.15s ease;
    }

    .settings-toggle[aria-checked="true"]::after {
      left: 28px;
    }

    .settings-toggle--disabled {
      cursor: not-allowed;
      pointer-events: none;
    }
    ```

    Component:
    ```tsx
    import { Modal } from './Modal'
    import { useGame, useSettings } from '../hooks/useGame'

    export function SettingsModal({ onClose }: { onClose: () => void }) {
      const { hardMode, colorblindMode, setHardMode, setColorblindMode } = useSettings()
      const guesses = useGame((s) => s.guesses)
      const hardModeDisabled = guesses.length > 0

      return (
        <Modal onClose={onClose}>
          <button className="modal-close" onClick={onClose} aria-label="Close">×</button>
          <h2 style={{ textAlign: 'center', fontSize: 24, fontWeight: 600, margin: '0 0 8px' }}>
            Settings
          </h2>
          <div>
            <div className="settings-row" style={hardModeDisabled ? { opacity: 0.5 } : undefined}>
              <div>
                <div style={{ fontSize: 16, fontWeight: 600 }}>Hard Mode</div>
                {hardModeDisabled && (
                  <div style={{ fontSize: 12, color: 'var(--color-absent)', marginTop: 2 }}>
                    Can only be changed before a game starts.
                  </div>
                )}
              </div>
              <button
                role="switch"
                aria-checked={hardMode}
                className={`settings-toggle${hardModeDisabled ? ' settings-toggle--disabled' : ''}`}
                onClick={() => !hardModeDisabled && setHardMode(!hardMode)}
                aria-label="Hard mode"
              />
            </div>
            <div className="settings-row">
              <div>
                <div style={{ fontSize: 16, fontWeight: 600 }}>Colorblind Mode</div>
                <div style={{ fontSize: 12, color: 'var(--color-absent)', marginTop: 2 }}>
                  For improved color vision
                </div>
              </div>
              <button
                role="switch"
                aria-checked={colorblindMode}
                className="settings-toggle"
                onClick={() => setColorblindMode(!colorblindMode)}
                aria-label="Colorblind mode"
              />
            </div>
          </div>
        </Modal>
      )
    }
    ```
  </action>
  <verify>
    <automated>cd /home/rflor/longdle && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>WinAnimation, HowToPlayModal, SettingsModal exist and TypeScript compiles; CSS classes added to tiles.css</done>
  <acceptance_criteria>
    - `grep "export function WinAnimation" src/components/WinAnimation.tsx` returns 1 match
    - `grep "return null" src/components/WinAnimation.tsx` returns 1 match (no-op stub)
    - `grep "v3" src/components/WinAnimation.tsx` returns at least 1 match (JSDoc annotation)
    - `grep "WinAnimationProps" src/components/WinAnimation.tsx` returns at least 2 matches (interface + usage)
    - `grep "howtoplay-tile" src/styles/tiles.css` returns at least 4 matches (base class + 3 modifiers)
    - `grep "settings-toggle" src/styles/tiles.css` returns at least 4 matches
    - `grep "role=\"switch\"" src/components/SettingsModal.tsx` returns 2 matches (two toggles)
    - `grep "guesses.length > 0" src/components/SettingsModal.tsx` returns 1 match (hard mode disable condition)
    - `grep "Can only be changed" src/components/SettingsModal.tsx` returns 1 match
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create StatsModal with histogram and share button</name>
  <files>src/components/StatsModal.tsx</files>
  <read_first>
    - src/lib/storage.ts — readStats() function signature, Stats interface (guessDistribution length 7, index semantics)
    - src/lib/share.ts — generateShareText signature (guesses, dayIndex, colorblindMode) → string
    - src/hooks/useGame.ts — useGame state fields available: guesses, dayIndex, gameStatus
    - src/components/Modal.tsx — Modal wrapper usage pattern
  </read_first>
  <action>
    Create src/components/StatsModal.tsx. This modal reads stats directly from `readStats()` (D-09 — no Zustand for stats).

    Props: `{ onClose: () => void; onShare: () => void }` — share handler passed from App.tsx (keeps clipboard logic centralized in App.tsx, iOS Safari constraint).

    Stats numbers grid: 4 columns flexbox, `justify-content: space-around`. Each stat: number on top (24px weight 600), label below (12px weight 400 centered).

    Histogram: 7 rows (1 through 7). Bar width = `Math.max(8, Math.round((count / maxCount) * 100))`. Highlighted bar uses `.stats-bar--highlight` when `gameStatus === 'won'` AND `i === guesses.length - 1`.

    Win % formula: `Math.round((gamesWon / gamesPlayed) * 100)` or `0` if `gamesPlayed === 0` (avoid NaN).

    maxCount formula: `Math.max(...stats.guessDistribution, 1)` — trailing `1` prevents divide-by-zero (RESEARCH Pitfall 4).

    Add to src/styles/tiles.css (append after settings-toggle rules):
    ```css
    /* ───────── Stats modal histogram (D-10, D-11) ───────── */

    .stats-row {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 4px;
    }

    .stats-label {
      font-size: 12px;
      font-weight: 600;
      width: 16px;
      text-align: right;
      flex-shrink: 0;
    }

    .stats-bar {
      height: 24px;
      min-width: 8%;
      display: flex;
      align-items: center;
      justify-content: flex-end;
      padding-right: 8px;
      font-size: 12px;
      font-weight: 600;
      color: var(--color-text-inverse);
      background: var(--color-surface);
      box-sizing: border-box;
    }

    .stats-bar--highlight {
      background: var(--color-correct);
    }

    /* ───────── Share button (D-04) ───────── */

    .share-btn {
      display: block;
      width: 100%;
      height: 48px;
      background: var(--color-correct);
      color: var(--color-text-inverse);
      border: none;
      border-radius: 4px;
      font-size: 16px;
      font-weight: 600;
      text-transform: uppercase;
      cursor: pointer;
      margin-top: 16px;
    }

    .share-btn:active {
      opacity: 0.85;
    }
    ```

    StatsModal component:
    ```tsx
    import { Modal } from './Modal'
    import { useGame, useSettings } from '../hooks/useGame'
    import { readStats } from '../lib/storage'

    export function StatsModal({ onClose, onShare }: { onClose: () => void; onShare: () => void }) {
      const guesses = useGame((s) => s.guesses)
      const gameStatus = useGame((s) => s.gameStatus)
      const stats = readStats()
      const maxCount = Math.max(...stats.guessDistribution, 1)
      const winPct = stats.gamesPlayed === 0
        ? 0
        : Math.round((stats.gamesWon / stats.gamesPlayed) * 100)

      return (
        <Modal onClose={onClose}>
          <button className="modal-close" onClick={onClose} aria-label="Close">×</button>
          <h2 style={{ textAlign: 'center', fontSize: 24, fontWeight: 600, margin: '0 0 16px' }}>
            Statistics
          </h2>
          <div style={{ display: 'flex', justifyContent: 'space-around', marginBottom: 32 }}>
            {[
              { value: stats.gamesPlayed, label: 'Played' },
              { value: winPct, label: 'Win %' },
              { value: stats.currentStreak, label: 'Current Streak' },
              { value: stats.maxStreak, label: 'Max Streak' },
            ].map(({ value, label }) => (
              <div key={label} style={{ textAlign: 'center' }}>
                <div style={{ fontSize: 24, fontWeight: 600 }}>{value}</div>
                <div style={{ fontSize: 12, lineHeight: 1.3 }}>{label}</div>
              </div>
            ))}
          </div>
          <div style={{ fontSize: 16, fontWeight: 600, marginBottom: 8 }}>Guess Distribution</div>
          {stats.guessDistribution.map((count, i) => {
            const pct = Math.max(8, Math.round((count / maxCount) * 100))
            const isHighlight = gameStatus === 'won' && i === guesses.length - 1
            return (
              <div key={i} className="stats-row">
                <span className="stats-label">{i + 1}</span>
                <div
                  className={`stats-bar${isHighlight ? ' stats-bar--highlight' : ''}`}
                  style={{ width: `${pct}%` }}
                >
                  {count}
                </div>
              </div>
            )
          })}
          <button className="share-btn" onClick={onShare}>Share</button>
        </Modal>
      )
    }
    ```

    Note: `onShare` is passed from App.tsx. App.tsx owns the clipboard logic (synchronous generateShareText + writeText) so the iOS Safari constraint is handled at the App level, not inside the modal.
  </action>
  <verify>
    <automated>cd /home/rflor/longdle && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>StatsModal exists; TypeScript clean; histogram and share button present; CSS classes added</done>
  <acceptance_criteria>
    - `grep "export function StatsModal" src/components/StatsModal.tsx` returns 1 match
    - `grep "readStats" src/components/StatsModal.tsx` returns 1 match (direct call, no Zustand)
    - `grep "onShare" src/components/StatsModal.tsx` returns at least 2 matches (prop type + button onClick)
    - `grep "stats-bar--highlight" src/components/StatsModal.tsx` returns 1 match
    - `grep "Math.max.*guessDistribution.*1" src/components/StatsModal.tsx` returns 1 match (divide-by-zero guard)
    - `grep "gamesPlayed === 0" src/components/StatsModal.tsx` returns 1 match (win% zero guard)
    - `grep "stats-bar" src/styles/tiles.css` returns at least 3 matches (.stats-bar base + .stats-bar--highlight + .stats-label)
    - `grep "share-btn" src/styles/tiles.css` returns at least 2 matches
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → readStats() → StatsModal | stats are read from localStorage; all values are numbers (no HTML injection surface) |
| SettingsModal → useSettings → Zustand | settings reads are from trusted store; no user-provided strings rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | localStorage stats manipulation | accept | Stats are display-only; manipulating stored numbers only affects personal display; no shared scoreboard |
| T-02-04-02 | Denial of Service | guessDistribution divide-by-zero | mitigate | Math.max(...stats.guessDistribution, 1) ensures maxCount >= 1 always |
| T-02-04-03 | Information Disclosure | WinAnimationProps interface exported | accept | Props are game result data (dayIndex, won, guessCount) — no PII |
</threat_model>

<verification>
1. `npx tsc -b --noEmit` — exits 0 (all 4 files type-safe)
2. `grep "return null" src/components/WinAnimation.tsx` — 1 match (no-op stub)
3. `grep "readStats" src/components/StatsModal.tsx` — 1 match (direct read, no Zustand)
4. `grep "stats-bar--highlight" src/styles/tiles.css` — 1 match
5. `grep "howtoplay-tile" src/styles/tiles.css` — 4+ matches (base + 3 modifiers)
</verification>

<success_criteria>
- WinAnimation renders null; has WinAnimationProps interface; has v3 JSDoc
- HowToPlayModal: 3 example tiles (correct/present/absent divs), rules text, Modal wrapper, close button
- StatsModal: 4 stat numbers, 7 histogram bars (min 8% width), highlight on current game's row when won, share button
- SettingsModal: hard mode toggle disabled when guesses.length > 0, colorblind toggle always on, both use role="switch"
- TypeScript compiles cleanly across all 4 new files + tiles.css additions
</success_criteria>

<output>
After completion, create `.planning/phases/02-features/02-04-SUMMARY.md` following the summary template.
</output>
